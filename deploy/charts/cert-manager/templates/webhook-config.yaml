{{- $perComponentLimits := dict -}}
{{- if and .Values.webhook.config .Values.webhook.config.pemSizeLimitsConfig -}}
{{- $perComponentLimits = .Values.webhook.config.pemSizeLimitsConfig -}}
{{- end -}}
{{- $pemLimits := merge (dict) $perComponentLimits (default dict .Values.global.pemSizeLimitsConfig) -}}
{{- if or .Values.webhook.config $pemLimits -}}
{{- $config := default (dict) .Values.webhook.config -}}
{{- if $pemLimits -}}
{{- $_ := set $config "pemSizeLimitsConfig" $pemLimits -}}
{{- end -}}
{{- $_ := set $config "apiVersion" (default "webhook.config.cert-manager.io/v1alpha1" $config.apiVersion) -}}
{{- $_ := set $config "kind" (default "WebhookConfiguration" $config.kind) -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webhook.fullname" . }}
  namespace: {{ include "cert-manager.namespace" . }}
  labels:
    app: {{ include "webhook.name" . }}
    app.kubernetes.io/name: {{ include "webhook.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: "webhook"
    {{- include "labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- $config | toYaml | nindent 4 }}
{{- end -}}
